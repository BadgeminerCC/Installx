local function sequence()
    local ExternalReqirements = {}
    local env = {
        reqirement = function(name)
            local url = string.format("badgeminer/Installx")
            ExternalReqirements[i] = nil
        end
    }
end
